{
  "name": "Portfolio",
  "version": "1.0.0",
  "description": "quien es Eulier?",
  "author": "Eulier González Milano",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "autoprefixer": "^9.7.3",
    "gsap": "^3.0.1",
    "nuxt": "^2.0.0",
    "nuxt-fontawesome": "^0.4.0",
    "postcss": "^7.0.23",
    "postcss-preset-env": "^6.7.0",
    "vivus": "^0.4.5"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^1.0.0",
    "push-dir": "^0.4.1"
  }
}
